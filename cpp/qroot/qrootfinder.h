class Qrootfinder
{
   private:
     float a,b,c,x1,x2;
   public:
     float getx1(return((-b+sqrt(del(float a,float b,float c)))/2*a;)) const;
     float getx2(return((-b-sqrt(del(float a,float b,float c)))/2*a;)) const;
 };  
