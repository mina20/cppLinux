//program to print add,sub,multi,and devidion of numbers//


#include<stdio.h>

int main()
{
   int a,b,sum,sub,multi,devision;
   printf("Enter two numbers");
   scanf(("%d %d",&a,&b));
   for(sum=a+b);
  {
   printf(("Sum = %d",sum));
   }
   for(sub=a-b);
   {
    printf("sub=%d",sub);
    }
}
   
